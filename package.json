{
  "name": "briefer",
  "version": "1.0.0",
  "private": true,
  "description": "Daily calendar brief emailed every morning, built from an ICS feed and sent via Resend.",
  "type": "commonjs",
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "brief:local": "ts-node scripts/run-local.ts"
  },
  "dependencies": {
    "date-fns": "^3.6.0",
    "date-fns-tz": "^3.2.0",
    "dotenv": "^17.4.2",
    "node-ical": "^0.22.0",
    "resend": "^6.28.1",
    "rrule": "^2.8.1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "@vercel/node": "^9.0.1",
    "ts-node": "^10.9.2",
    "typescript": "^5.9.3"
  }
}
